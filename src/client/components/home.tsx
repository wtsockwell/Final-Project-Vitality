import * as React from 'react';
import Hero from './hero';
import home from 


// Home = (props: HomeProps) => {
    return (
        <h2>Home</h2>
    )
}

interface HomeProps { };

export default Home;